ul.pagination.justify-content-center
    a(href="#").page-link 처음
    li.page-item
        a(href="#").page-link.prev 이전

    if pages
        each page, index in pages
            p= page.page
            if currentpage = page.number
                li.page-item.active
                    a(href="#").page-link= page.number
            else
                li.page-item
                    a(href="#").page-link= page.number
    a(href="#").page-link 끝
    